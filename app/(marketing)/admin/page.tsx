import { ComingSoon } from '@/components/marketing/ComingSoon';

export const metadata = { title: 'Member Login' };

export default function Page() {
  return (
    <ComingSoon
      title="Member Login"
      phase="Phase 5"
      blurb="The member and committee portal is not yet available. Committee members currently review applications through the single-use links emailed to them."
    />
  );
}
